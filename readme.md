# Pub/Sub Visualisation

Front-end to visualise pub/sub topology.

### Run

- `$ yarn run start`
Starts a web server and refreshes whenever a source file is changed.
- `$ yarn run build`
Builds the `dist/` folder ready for production.

### CORS
